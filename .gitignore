# Node.js Dependencies
node_modules/

# Environment Variables
.env

# Config Files (User-Specific)
config/testConfig.json

# Reports & Logs
reports/
allure-results/
allure-report/
.allure/
logs/
execution.log

# Persisted LocatorStore entries (runtime state, not source — see README
# Project Structure, which already documented this as gitignored).
data/

# Playwright Traces & Screenshots
trace.zip
*.png
*.jpg
*.jpeg
# ...except committed README demo stills (see docs/demo/).
!docs/demo/*.png

# IDE & OS-Specific Files
.vscode/
.DS_Store
Thumbs.db

# Git & CI/CD Artifacts
coverage/
.npm/

# Internal engineering handoff — not for public visibility (this repo is public).
# Kept locally for continuity between engineers/sessions, never committed.
#HANDOFF.md
